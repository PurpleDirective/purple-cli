# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
venv/

# Data (never commit)
memory/*.db
memory/*.db-wal
memory/*.db-shm
*.db

# OS
.DS_Store
*.swp
*.swo

# Personal config (not committed)
identity/identity.md

# Secrets
.env
secrets/
credentials/

# Local config (personal paths -- use mcp.example.json as template)
config/mcp.json

# User data (never commit)
cli/history
cli/mcp.log
sessions/*.jsonl
eval/log.csv

# Teaching data (structure committed, content is per-installation)
teaching/queue/*.md
teaching/compiled/*
teaching/audit/*
teaching/verify/*
teaching/finetune/*
teaching/transcripts/
!teaching/**/.gitkeep

# Eval results (code ships, data stays local)
eval/*-results*/
eval/frontier/
eval/v6-comparison-*/
eval/bcb-results/
eval/evalplus-results/
eval/log.csv

# Runtime artifacts
hooks/hook.log
*.db-shm
*.db-wal

# Retired server backups
*.retired

# Demo source (keep generated outputs)
demo.tape
demo.webm
